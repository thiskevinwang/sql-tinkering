{
  "transforms": [
    {
      "mode": "sql",
      "include": "**/*.sql",
      "emitTemplate": "{{dir}}/{{name}}.queries.ts"
    }
  ],
  "srcDir": "./sql",
  "failOnError": false,
  "db": {
    "host": "localhost",
    "port": 8080,
    "user": "postgres",
    "dbName": "postgres",
    "password": "mysecretpassword"
  }
}
